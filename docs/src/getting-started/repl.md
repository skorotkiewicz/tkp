# REPL

Start the interactive REPL:

```bash
tkp repl
```

```
TKP (tkp) REPL v0.1.0
exit: Ctrl+D or 'exit' kute

tkp> ijo x = 42
tkp> toki(x * 2)
84
tkp> exit

toki a!
```

The REPL maintains state between lines — variables and functions persist across inputs.
